[pytest]
minversion = 8.0
testpaths = tests

addopts =
    -ra
    --strict-markers
    --strict-config

norecursedirs =
    .venv
    .git
    */templates
    */__pycache__

markers =
    calibration: pins exact counts against the private USC corpus; skips cleanly when it is unavailable
